export enum Direction {
	Up = "Up",
	Down = "Down",
	Left = "Left",
	Right = "Right"
};

export enum Rotate {
	CounterClockwise = "CounterClockwise",
	Clockwise = "Clockwise"
};

export enum Color {
	Purple = 0,
	Blue = 1,
	Orange = 2,
	Red = 3,
	Green = 4
};